import 'package:flutter/material.dart';

class ThemeSuccessAction {
  final ThemeMode themeMode;

  ThemeSuccessAction(this.themeMode);
}

class ThemeSaveAction {
  final ThemeMode themeMode;

  ThemeSaveAction({required this.themeMode});
}
